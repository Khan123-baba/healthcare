<html lang="en">  
<head>  
    <meta charset="UTF-8">  
    <meta name="viewport" content="width=device-width, initial-scale=1.0">  
    <meta http-equiv="X-UA-Compatible" content="ie=edge">  
    <title>Document</title>  
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">  
    <script src="/jquery/jquery.js"></script>  
</head>     
<body>  
    <div class="container" style="margin-top: 50px;">  
        <div class="nav justify-content-center">  
            <div class="card">  
                <h5 class="card-header text-center">ToDo List</h5>  
                <div class="card-body">
                    <Form method="POST" id="addtask">  
                        <div class="form-group text-center">  
                            <label for="Task">Enter The Task</label>  
                            <input type="text" class="form-control" name="specilityname" id="task" required> 
                            <p id="error_message">your task is adding</p> 
                          </div> 
                     <div class="text-center"><button class="btn btn-lg btn-success addbt">Add Task</button></div>   
                    </Form>
                    </div>  

              </div>  
        </div><br>  
    <div class="nav justify-content-center tblData" style="overflow-y:scroll; height: 200px;">  

        <table class="table table-hover">  
            <thead>  
            <tr>  
                <th>  
                    s.no  
                </th>  
                <th>  
                    Task  
                </th>  
                <th>  
                    delete  
                </th>  
            </tr>  
        </thead>  
        <tbody >  
        </tbody>  
        </table>  
    </div>  
</div>  
    <script>
        $("#addtask").submit(function (e) {
            $.ajax({
                type:"POST",
                url:"doctorSpecility",
                processData:false,
                contentType:false,
                data:JSON.stringify(jsonNewValue),
                success:function(data){

                    if(data.success==true){
                        $('#error_message').text('Task Added');                        
                    }
                }
            });
        });</script>  
</body>  
</html>  